# 设置页面重新设计方案

## 设计目标
参考 AIRI 设置页面的卡片式设计，实现流畅、美观的设置界面。

## 设计分析（基于 AIRI 截图）

### 布局结构
- **卡片式布局**：每个设置模块为一个独立卡片
- **垂直排列**：卡片从上到下依次排列
- **左右分区**：左侧文字（标题+描述），右侧图标

### 视觉风格
- **浅色主题**：整体采用浅灰色/白色背景
- **卡片样式**：
  - 圆角：8-12px
  - 边框：虚线/点阵纹理效果
  - 背景：浅灰色渐变或纯色
- **图标风格**：抽象几何图标，灰色/蓝色
- **文字层级**：标题粗体大号，描述小号灰色

### 模块划分
1. **角色卡**（特殊高亮卡片）
   - 使用场景：快速配置角色预设
   - 设计：蓝色背景，醒目位置

2. **机体模块**
   - 包含：思维、视觉、言语综合、游戏等

3. **场景**
   - 自定义角色的虚拟环境

4. **角色模型**
   - Live2D、VRM、Spine 等模型管理

5. **记忆体**
   - 记忆存储和策略配置

6. **服务来源**
   - LLM、语音合成、语音识别服务配置

## 实现方案

### 组件结构
```
SettingsPanel
├── Header（标题栏）
├── SettingsCard（通用卡片组件）
│   ├── icon（右侧图标）
│   ├── title（标题）
│   └── description（描述）
└── Footer（底部操作）
```

### 配色方案
- 主背景：#f5f5f5 或 #ffffff
- 卡片背景：#ffffff 或 rgba(255,255,255,0.8)
- 卡片边框：#e5e7eb（点阵虚线效果）
- 主标题：#1f2937
- 描述文字：#6b7280
- 高亮卡片：#dbeafe（蓝色背景）

### 动画效果
- 卡片悬停：轻微放大 + 阴影增强
- 卡片点击：缩放反馈
- 页面切换：淡入淡出

### 响应式设计
- 桌面端：固定宽度居中
- 移动端：全屏宽度

## 模块映射（Desk Pet → AIRI 风格）

| Desk Pet 当前设置 | AIRI 风格模块 |
|------------------|--------------|
| AI 配置（API Key/Model） | 服务来源 |
| 主题设置 | 机体模块 → 外观 |
| 淡出透明度 | 机体模块 → 交互 |
| 规则管理 | 记忆体 |
| 角色模型切换 | 角色模型 |
| 网页管理后台 | 服务来源 → 高级 |

---

## 已实现功能（2026-08-03）

### 设置搜索框

PageHeader 标题右侧新增搜索框（`src/settings/components/SettingsSearch.tsx`）：

- **数据源**：`settingsTree`（`routes.tsx` 导出的路由单一真相源），递归扁平化全部设置条目
- **匹配范围**：标题（中文）/ 副标题（英文）/ 描述，大小写不敏感
- **交互**：
  - 输入即时过滤，结果最多 12 条
  - 下拉结果展示图标 + 标题 + 副标题/描述 + 所属分组
  - 点击 / Enter 跳转对应设置页，↑↓ 键盘选择，Esc / 点击外部关闭
  - 清空按钮一键重置
- **挂载位置**：`PageHeader` 新增 `actions` 插槽，`SettingsLayout` 传入，所有设置页统一可见

### 图标完整性

设置页所有图标使用 `@iconify/react` + 本地 `@iconify-json/solar` 包离线加载。
图标名以 `node_modules/@iconify-json/solar/icons.json` 为准，新增图标引用需先校验存在性。
