# ============================================================================
# 模型权重 / 大型运行时依赖 —— 绝不打进安装包
# ----------------------------------------------------------------------------
# 设计前提：本应用打包产物【不含任何模型权重文件】。权重由用户本地放置或在
# 配置向导里选择目录，运行时按需加载。即便将来把 server/ 加进 bundle.resources，
# 以下规则也会把权重剔除，避免安装包体积爆炸 / 误带版权权重。
# 语法同 .gitignore，匹配根目录为 src-tauri。
# ============================================================================

# ── 权重文件类型 ──
**/*.ckpt
**/*.pth
**/*.pt
**/*.safetensors
**/*.bin
**/*.onnx
**/*.gguf
**/*.ggml

# ── 权重 / 模型目录（GPT-SoVITS / ASR / HF 缓存等）──
**/pretrained_models/**
**/pretrained/**
**/models--/**
**/SoVITS_weights*/
**/GPT_weights*/
**/checkpoints/**
**/weights/**

# ── 本地后端权重目录（server/ 若将来被打包，仅剔除权重，保留 .py 代码）──
**/server/models/**
**/server/.venv/**

# ── Python venv（巨大，绝不打包）──
**/venv/**
**/.venv/**

# ── 构建 / VCS 垃圾（Tauri 多数已默认忽略，列出以防万一）──
**/node_modules/**
**/target/**
**/.git/**
**/__pycache__/**
**/*.pyc
